# RAG + ReAct 智能问答系统开发指导书

**版本号：v1.0**
**编制日期：2025年10月10日**
**编写人：—**
**审核人：—**
**批准人：—**

## 1. 项目概述

### 1.1 项目背景

本项目旨在开发一个基于 RAG（Retrieval-Augmented Generation）技术并结合 ReAct（Reason + Act）智能代理范式的知识问答系统。该系统需要处理一本数百页、不可直接复制的 PDF 书籍，通过结构化清洗与嵌入检索，实现高质量的语义问答。

随着企业知识管理需求的不断增长，传统的关键词搜索已无法满足用户对精准信息获取的需求。本系统通过结合最新的检索增强生成技术和智能代理架构，旨在构建一个能够理解用户意图、自主决策并调用多种工具的智能问答平台。

### 1.2 项目意义

- **技术创新**：结合RAG和ReAct两种前沿AI技术，实现更智能的知识问答
- **实用价值**：解决传统PDF文档无法直接检索和问答的问题
- **商业价值**：提供可复用的智能问答解决方案，适用于各类企业知识库
- **技术前瞻性**：采用模块化架构，支持未来技术升级和功能扩展

## 2. 项目目标

### 2.1 核心目标

1. **文档处理**：将原始PDF文档转换为结构化文本数据
2. **RAG系统构建**：构建一个结合关键字检索与矢量检索的RAG系统
3. **智能代理**：采用ReAct架构设计智能代理，使其具备自我决策与工具调用能力
4. **多模型支持**：支持多种大型语言模型接口
5. **部署展示**：提供可快速部署的前端展示界面与Docker化运行环境
6. **性能评估**：构建测评集并生成系统性能指标（召回率、准确率、F1值）

### 2.2 技术指标

- **检索准确率**：≥85%
- **问答准确率**：≥80%
- **响应时间**：单次检索≤5秒
- **并发支持**：支持10个并发查询
- **可扩展性**：支持水平扩展至百万级文档

## 3. 系统总体架构

### 3.1 架构设计原则

- **模块化**：各功能模块独立，便于维护和扩展
- **可扩展**：支持新模型、新功能的快速集成
- **高性能**：优化检索算法，保证响应速度
- **可靠性**：具备容错机制和降级策略

### 3.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    前端展示层                                │
│              ┌───────────────────────┐                      │
│              │    Web前端界面         │                      │
│              │  (查询输入/结果展示)    │                      │
│              └──────────┬──────────────┘                      │
└─────────────────────────┼──────────────────────────────────────┘
                          │
┌─────────────────────────┼──────────────────────────────────────┐
│                    API网关层                               │
│              ┌──────────▼──────────────┐                      │
│              │    FastAPI服务          │                      │
│              │  (RESTful API接口)      │                      │
│              └──────────┬──────────────┘                      │
└─────────────────────────┼──────────────────────────────────────┘
                          │
┌─────────────────────────┼──────────────────────────────────────┐
│                  业务逻辑层                                │
│              ┌──────────▼──────────────┐                      │
│              │    ReAct智能代理        │                      │
│              │  (LangGraph框架)        │                      │
│              └──────────┬──────────────┘                      │
│                         │                                  │
│              ┌──────────▼──────────────┐                      │
│              │    RAG问答引擎          │                      │
│              │  (检索+生成)            │                      │
│              └──────────┬──────────────┘                      │
└─────────────────────────┼──────────────────────────────────────┘
                          │
┌─────────────────────────┼──────────────────────────────────────┐
│                  数据存储层                                │
│              ┌──────────▼──────────────┐                      │
│              │    矢量数据库            │                      │
│              │    (Milvus)             │                      │
│              └──────────┬──────────────┘                      │
│                         │                                  │
│              ┌──────────▼──────────────┐                      │
│              │    全文搜索引擎          │                      │
│              │    (Elasticsearch)      │                      │
│              └──────────┬──────────────┘                      │
│                         │                                  │
│              ┌──────────▼──────────────┐                      │
│              │    结构化数据            │                      │
│              │    (PostgreSQL)         │                      │
│              └─────────────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 核心模块说明

| 模块 | 功能描述 | 技术方案 |
|------|----------|----------|
| 数据处理模块 | PDF文本抽取与清洗，分段结构化 | Python + PDF解析库 |
| 索引与检索模块 | 关键词检索 + 矢量检索 | Elasticsearch + Milvus |
| 语义建模模块 | 矢量嵌入与Re-rank | Jina 0.6B / 千问3 0.6B |
| 问答推理模块 | 结合RAG输出的多模型问答 | DeepSeek Reasoner / 千问3 Max / 千问3 80B |
| ReAct智能代理模块 | 自主Query改写、多轮检索决策 | LangGraph框架（Python） |
| 部署与展示模块 | 前端展示与容器化部署 | Cloud Code + Dockerfile + docker-compose |

## 4. 详细需求

### 4.1 功能性需求

#### 4.1.1 数据处理需求

**需求描述**：
- PDF转换为结构化可检索文本
- 支持分章节分页处理
- 输出JSON/文本段索引格式

**技术要求**：
```python
# 核心处理流程
PDF文件 → OCR识别 → 文本清洗 → 结构化分段 → 元数据提取 → 索引构建
```

**具体实现**：
- 使用PyMuPDF或pdfplumber进行PDF解析
- 集成Tesseract OCR处理扫描版PDF
- 实现智能分段算法，保持语义完整性
- 提取章节、标题、页码等元数据

#### 4.1.2 检索与召回需求

**需求描述**：
- Elasticsearch用于关键词检索
- Milvus用于矢量检索
- 支持联合召回策略

**技术方案**：
```python
# 检索流程
def hybrid_retrieval(query, top_k=10):
    # 1. 关键词检索
    keyword_results = elasticsearch_search(query, top_k=5)

    # 2. 矢量检索
    vector_results = milvus_search(embedding(query), top_k=10)

    # 3. 结果融合与重排序
    final_results = rerank_and_fuse(keyword_results, vector_results)

    return final_results
```

**召回策略**：
- 关键词检索：使用BM25算法，支持模糊匹配
- 矢量检索：基于余弦相似度，支持ANN搜索
- 联合召回：采用RRF（Reciprocal Rank Fusion）算法

#### 4.1.3 嵌入与排序需求

**需求描述**：
- Jina 0.6B与千问3 0.6B模型对比
- 支持动态切换配置
- 实现Re-rank机制

**模型配置**：
```python
# 嵌入模型配置
EMBEDDING_MODELS = {
    "jina_0.6b": {
        "model_name": "jina-embeddings-v2-base-zh",
        "dimension": 768,
        "max_length": 8192
    },
    "qwen3_0.6b": {
        "model_name": "Qwen3-0.6B-embedding",
        "dimension": 768,
        "max_length": 32768
    }
}
```

#### 4.1.4 问答生成需求

**需求描述**：
- 基于RAG输出调用大模型API
- 支持DeepSeek Reasoner、千问3 Max、千问3 80B
- 实现多模型切换和负载均衡

**模型接入配置**：
```python
# LLM模型配置
LLM_MODELS = {
    "deepseek_reasoner": {
        "api_base": "https://api.deepseek.com/v1",
        "model_name": "deepseek-reasoner",
        "max_tokens": 4096,
        "temperature": 0.7
    },
    "qwen3_max": {
        "api_base": "https://dashscope.aliyuncs.com/api/v1",
        "model_name": "qwen3-max",
        "max_tokens": 8192,
        "temperature": 0.8
    },
    "qwen3_80b": {
        "api_base": "https://dashscope.aliyuncs.com/api/v1",
        "model_name": "qwen3-80b",
        "max_tokens": 8192,
        "temperature": 0.9
    }
}
```

#### 4.1.5 ReAct智能代理需求

**需求描述**：
- 基于LangGraph实现Reason/Decision/Action节点
- 支持Query改写与多轮检索
- 实现自主决策机制

**代理架构设计**：
```python
# ReAct代理节点定义
class ReActAgent:
    def __init__(self):
        self.nodes = {
            "reason": self.reason_node,
            "decide": self.decision_node,
            "act": self.action_node,
            "observe": self.observation_node
        }

    def reason_node(self, state):
        # 推理当前状态和目标
        pass

    def decision_node(self, state):
        # 决定下一步行动
        pass

    def action_node(self, state):
        # 执行具体行动（检索、问答等）
        pass

    def observation_node(self, state):
        # 观察行动结果
        pass
```

#### 4.1.6 前端展示需求

**需求描述**：
- 简易Web前端界面
- 支持查询输入与结果展示
- 加入历史查询记录功能
- 支持3种不同的模型api进行召回，可自选api，分别是千问3next80b,千问3-max（暂时还没有，可以先空着），deepseek-reasonal，可自己配置api和url
- 可选择返回结果带图片或者不带图片
- 若结果带有图片，则图片显示在网页侧边，每张图片都可以点击放大查看
- RAG系统返回的结果呈列表形式放在最下方，每一个参考都有详细标注，如页面、相关度等等信息

**界面设计**：
```html
<!-- 前端界面结构 -->
<div class="main-container">
    <div class="search-section">
        <input type="text" id="query-input" placeholder="请输入您的问题...">
        <button id="search-btn">搜索</button>
    </div>

    <div class="history-section">
        <h3>历史查询</h3>
        <ul id="history-list"></ul>
    </div>

    <div class="results-section">
        <div class="answer-box">
            <h3>AI回答</h3>
            <div id="answer-content"></div>
        </div>

        <div class="sources-box">
            <h3>参考来源</h3>
            <div id="sources-list"></div>
        </div>
    </div>
</div>
```

#### 4.1.7 部署与容器化需求

**需求描述**：
- 提供Dockerfile和docker-compose配置
- 实现一键部署
- 支持环境变量配置

**Docker配置示例**：
```dockerfile
# Dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```yaml
# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - MILVUS_URL=http://milvus:19530
    depends_on:
      - elasticsearch
      - milvus
      - postgres

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.8.0
    environment:
      - discovery.type=single-node
    ports:
      - "9200:9200"

  milvus:
    image: milvusdb/milvus:v2.3.0
    ports:
      - "19530:19530"

  postgres:
    image: postgres:15
    environment:
      - POSTGRES_DB=rag_system
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
```

#### 4.1.8 系统评测需求（可选）

**需求描述**：
- 使用RAGAS构建评测集
- 输出准确率、召回率、F1值
- 生成性能评估报告

**评测指标**：
```python
# 评测指标定义
metrics = {
    "context_relevancy": "上下文相关性",
    "context_recall": "上下文召回率",
    "context_precision": "上下文精确率",
    "answer_relevancy": "答案相关性",
    "answer_correctness": "答案正确性",
    "answer_similarity": "答案相似度"
}
```

### 4.2 非功能性需求

#### 4.2.1 性能需求

| 指标 | 要求 | 说明 |
|------|------|------|
| 响应时间 | ≤5秒 | 单次检索响应时间 |
| 并发处理 | ≥10个 | 同时处理的查询数量 |
| 吞吐量 | ≥100 QPS | 每秒查询数 |
| 可用性 | ≥99.9% | 系统可用性 |

#### 4.2.2 可扩展性需求

- **水平扩展**：支持通过增加节点提升处理能力
- **垂直扩展**：支持单机资源配置升级
- **模型扩展**：支持新模型的快速接入
- **数据源扩展**：支持多种格式文档的处理

#### 4.2.3 安全性需求

- **API密钥管理**：使用环境变量管理API密钥
- **数据加密**：敏感数据传输和存储加密
- **访问控制**：实现用户认证和授权机制
- **审计日志**：记录关键操作和访问日志

#### 4.2.4 可维护性需求

- **模块化结构**：各功能模块独立，便于维护
- **版本控制**：使用Git进行代码版本管理
- **文档完整**：提供详细的技术文档和API文档
- **监控告警**：实现系统监控和异常告警

#### 4.2.5 部署兼容性需求

- **操作系统**：兼容Linux/macOS/Windows
- **容器化**：支持Docker和Kubernetes部署
- **云平台**：支持主流云平台部署
- **网络环境**：支持内网和公网部署

## 5. 开发与交付要求

### 5.1 时间安排

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|----------|--------|
| 阶段1 | T0+3天 | 框架搭建与数据清洗 | 基础框架、清洗后的数据 |
| 阶段2 | T0+5天 | RAG检索逻辑 | 检索模块、测试报告 |
| 阶段3 | T0+7天 | ReAct代理与模型接入 | 智能代理、模型接口 |
| 阶段4 | T0+10天 | Docker化与测试 | 容器化部署、测试报告 |
| 阶段5 | 下周四/五 | 验收 | 完整系统、文档 |

### 5.2 里程碑检查点

**里程碑1（T0+3天）**：
- ✅ 项目环境搭建完成
- ✅ PDF解析模块开发完成
- ✅ 基础数据清洗完成
- ✅ Elasticsearch和Milvus环境搭建

**里程碑2（T0+5天）**：
- ✅ RAG检索核心逻辑完成
- ✅ 关键词检索功能实现
- ✅ 矢量检索功能实现
- ✅ 混合检索策略实现

**里程碑3（T0+7天）**：
- ✅ ReAct代理框架搭建
- ✅ 多模型接口集成
- ✅ 问答生成功能实现
- ✅ 基础测试用例通过

**里程碑4（T0+10天）**：
- ✅ Docker容器化完成
- ✅ 系统集成测试通过
- ✅ 性能测试完成
- ✅ 文档编写完成

### 5.3 交付内容清单

#### 5.3.1 系统源代码

```
rag-react-system/
├── src/
│   ├── data_processing/          # 数据处理模块
│   │   ├── pdf_parser.py
│   │   ├── text_cleaner.py
│   │   └── chunk_splitter.py
│   ├── retrieval/                # 检索模块
│   │   ├── elasticsearch_client.py
│   │   ├── milvus_client.py
│   │   └── hybrid_retriever.py
│   ├── embedding/                # 嵌入模块
│   │   ├── embedding_models.py
│   │   └── reranker.py
│   ├── llm/                      # 大模型接口
│   │   ├── model_clients.py
│   │   └── prompt_templates.py
│   ├── react_agent/              # ReAct代理
│   │   ├── agent_nodes.py
│   │   ├── state_manager.py
│   │   └── tool_kit.py
│   ├── api/                      # API接口
│   │   ├── main.py
│   │   ├── routers/
│   │   └── models/
│   └── frontend/                 # 前端代码
│       ├── static/
│       └── templates/
├── tests/                        # 测试代码
├── config/                       # 配置文件
├── docs/                         # 文档
├── scripts/                      # 脚本文件
└── requirements.txt              # 依赖包
```

#### 5.3.2 部署文件

- **Dockerfile**：应用容器化配置
- **docker-compose.yml**：多服务编排配置
- **deploy.sh**：一键部署脚本
- **nginx.conf**：反向代理配置

#### 5.3.3 文档资料

- **技术文档**：系统架构、模块设计、API文档
- **用户手册**：系统使用指南、常见问题
- **部署指南**：环境要求、部署步骤、配置说明
- **测试报告**：功能测试、性能测试、安全测试

#### 5.3.4 可选交付物

- **性能评估报告**：使用RAGAS生成的评测报告
- **演示视频**：系统功能演示
- **培训材料**：用户使用培训PPT

### 5.4 质量标准

#### 5.4.1 代码质量要求

- **代码规范**：遵循PEP 8 Python编码规范
- **代码覆盖率**：单元测试覆盖率≥80%
- **代码审查**：关键模块需通过代码审查
- **文档注释**：函数和类必须有完整的docstring

#### 5.4.2 性能要求

- **响应时间**：95%的请求响应时间≤5秒
- **并发能力**：支持≥10个并发查询
- **资源使用**：内存占用≤8GB，CPU使用率≤70%
- **稳定性**：连续运行24小时无故障

#### 5.4.3 安全要求

- **数据安全**：敏感数据加密存储和传输
- **访问控制**：实现用户认证和权限管理
- **输入验证**：防止SQL注入、XSS等安全漏洞
- **日志审计**：记录关键操作和异常信息

## 6. 待确认问题与确认结果

| 序号 | 问题 | 状态 | 说明 |
|------|------|------|------|
| 1 | "MS"是否确认为Milvus矢量数据库？ | ✅已确认 | 使用Milvus作为矢量数据库 |
| 2 | ReAct框架是否为LangGraph？ | ✅已确认 | 使用LangGraph实现 |
| 3 | 是否需要支持多模型动态切换？ | ⚙️暂不确定 | 可选功能，可后续扩展 |
| 4 | 前端是否需加入历史查询记录？ | ✅已确认 | 需支持历史记录功能 |
| 5 | 是否计划生成性能评估报告？ | ✅已确认 | 使用RAGAS生成评估报告 |
| 6 | PDF文件是否包含图片？ | ✅已确认 | 需考虑OCR或图文处理 |
| 7 | 是否需要使用开源模型托管平台？ | ❌不需要 | 全部模型通过API调用接入 |

### 6.1 待进一步确认的技术细节

1. **PDF处理策略**：
   - 是否所有PDF都需要OCR处理？
   - 图片中的文字如何处理？
   - 表格数据的结构化方案？

2. **检索优化**：
   - 是否需要实现查询扩展？
   - 重排序算法的具体选择？
   - 检索结果的数量配置？

3. **模型配置**：
   - 各模型的具体调用成本？
   - 模型切换的触发条件？
   - 模型响应超时处理？

4. **部署环境**：
   - 目标服务器的配置要求？
   - 是否需要高可用部署？
   - 数据备份和恢复策略？

## 7. 风险评估与应对策略

### 7.1 技术风险

| 风险点 | 影响程度 | 应对策略 |
|--------|----------|----------|
| PDF解析准确性 | 高 | 使用多种解析工具对比验证，建立质量检查机制 |
| 检索效果不佳 | 高 | 实现多种检索策略，支持参数调优和A/B测试 |
| 大模型API稳定性 | 中 | 实现多模型备份和降级策略，增加缓存机制 |
| 系统性能瓶颈 | 中 | 进行性能压测，优化关键路径，支持水平扩展 |

### 7.2 项目风险

| 风险点 | 影响程度 | 应对策略 |
|--------|----------|----------|
| 开发进度延迟 | 高 | 制定详细的里程碑计划，定期检查进度 |
| 需求变更频繁 | 中 | 建立需求变更控制流程，评估变更影响 |
| 团队协作问题 | 中 | 建立有效的沟通机制，定期同步进展 |
| 技术方案变更 | 低 | 保持技术方案灵活性，预留扩展空间 |

## 8. 后续发展规划

### 8.1 短期优化（1-2月）

- **性能优化**：提升检索速度，优化内存使用
- **功能增强**：增加多语言支持，完善用户界面
- **模型升级**：集成更多优质大模型，提升问答质量
- **监控完善**：增加系统监控和告警功能

### 8.2 中期扩展（3-6月）

- **多模态支持**：支持图片、音频、视频内容处理
- **知识图谱**：构建领域知识图谱，增强推理能力
- **个性化推荐**：基于用户行为的个性化问答推荐
- **移动端**：开发移动端应用，支持离线使用

### 8.3 长期愿景（6月+）

- **企业级解决方案**：打造完整的企业知识管理平台
- **行业定制**：针对特定行业提供定制化解决方案
- **智能分析**：提供数据分析和洞察功能
- **生态建设**：建立开发者生态，支持第三方插件

---

**文档修订记录**

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0 | 2025-10-10 | - | 初始版本创建 |

**联系方式**

如有任何问题或建议，请联系项目相关负责人。

---

*本文档为RAG + ReAct智能问答系统项目的开发指导书，详细描述了项目的需求、架构、开发计划等内容。请项目团队成员仔细阅读并遵照执行。*